<%= form_with(model: classificacao, local: true) do |form| %>
  <% if classificacao.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(classificacao.errors.count, "error") %> prohibited this classificacao from being saved:</h2>

      <ul>
      <% classificacao.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :id %>
    <%= form.text_field :id, id: :classificacao_id %>
  </div>

  <div class="field">
    <%= form.label :classificacao %>
    <%= form.text_field :classificacao, id: :classificacao_classificacao %>
  </div>

  <div class="field">
    <%= form.label :cor %>
    <%= form.text_field :cor, id: :classificacao_cor %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
